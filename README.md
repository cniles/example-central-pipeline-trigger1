# example-central-pipeline-trigger1
Actual source code for the project. No Jenkinsfile is present. This
repository will be cloned onto the build agent when its webhook
initiates the build on Jenkins.
